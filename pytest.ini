[pytest]
pythonpath = .
env_override_existing_values = 1
env_files = .test.env
asyncio_mode = auto
addopts = -p no:anyio
markers =
    integration: mark test as integration test (requires Docker/Testcontainers)